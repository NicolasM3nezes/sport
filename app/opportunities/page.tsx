import {opportunities} from '@/lib/data';export const dynamic='force-dynamic';export default async function Page(){let rows:any[]=[];try{rows=await opportunities()}catch{}return <><div className="pagehead"><span className="eyebrow">VALUE FINDER</span><h2>Oportunidades estatísticas</h2><p>Somente mercados com odd real e probabilidade do modelo acima da implícita.</p></div><div className="table"><div className="tr head"><span>Jogo</span><span>Mercado</span><span>Modelo</span><span>Odd</span><span>Edge</span></div>{rows.length?rows.map(r=>{const m=r.prediction_run?.match;return <div className="tr" key={r.id}><span>{m?.home_team?.name} × {m?.away_team?.name}</span><span>{r.market} · {r.selection}</span><span>{(r.probability*100).toFixed(1)}%</span><span>{r.bookmaker_odd?.toFixed(2)}</span><span>+{(r.edge*100).toFixed(1)} pp</span></div>}):<div className="empty">Nenhuma oportunidade com preço de mercado disponível.</div>}</div></>}
