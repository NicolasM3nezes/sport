import {todayMatches} from '@/lib/data';import {MatchCard} from '@/components/MatchCard';export const dynamic='force-dynamic';export default async function Page(){let data:any[]=[];try{data=await todayMatches()}catch{}return <><div className="pagehead"><span className="eyebrow">AGENDA</span><h2>Jogos de hoje</h2><p>Horários exibidos no fuso America/Sao_Paulo.</p></div><section className="grid">{data.length?data.map(m=><MatchCard key={m.id} m={m}/>):<div className="empty">Nenhum jogo disponível para hoje.</div>}</section></>}
