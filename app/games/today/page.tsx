import {todayMatches} from '@/lib/data';import {MatchCard} from '@/components/MatchCard';export const dynamic='force-dynamic';
export default async function Page(){let data:any[]=[];try{data=await todayMatches()}catch{}const analyzed=data.filter(m=>m.analysis).length;return <><div className="pagehead"><span className="eyebrow">AGENDA + ANÁLISE</span><h2>Jogos de hoje</h2><p>Horários em America/Sao_Paulo. {analyzed} de {data.length} partidas possuem probabilidade matemática disponível agora.</p></div><section className="grid">{data.length?data.map(m=><MatchCard key={m.id} m={m}/>):<div className="empty">Nenhum jogo disponível para hoje.</div>}</section></>}
