import {upcoming} from '@/lib/data';import {MatchCard} from '@/components/MatchCard';export const dynamic='force-dynamic';export default async function Page(){let data:any[]=[];try{data=await upcoming(60)}catch{}return <><div className="pagehead"><span className="eyebrow">PARTIDAS</span><h2>Próximos jogos</h2><p>Agenda sincronizada da API esportiva.</p></div><section className="grid">{data.length?data.map(m=><MatchCard key={m.id} m={m}/>):<div className="empty">Nenhuma partida sincronizada.</div>}</section></>}
