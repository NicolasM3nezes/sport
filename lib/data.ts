import {db} from './supabase';
export type MatchRow={id:string;kickoff_at:string;status_code:string;status_label:string;home_score:number|null;away_score:number|null;venue:string|null;country:string|null;league:any;home_team:any;away_team:any};
const selectMatch='id,kickoff_at,status_code,status_label,home_score,away_score,venue,country,league:leagues(name,country,logo_url),home_team:teams!matches_home_team_id_fkey(name,logo_url),away_team:teams!matches_away_team_id_fkey(name,logo_url)';
export async function todayMatches(){const now=new Date();const parts=new Intl.DateTimeFormat('en-CA',{timeZone:'America/Sao_Paulo',year:'numeric',month:'2-digit',day:'2-digit'}).format(now);const start=new Date(parts+'T00:00:00-03:00').toISOString();const end=new Date(parts+'T23:59:59-03:00').toISOString();const {data,error}=await db.from('matches').select(selectMatch).gte('kickoff_at',start).lte('kickoff_at',end).order('kickoff_at');if(error)throw error;return (data||[]) as unknown as MatchRow[]}
export async function upcoming(limit=12){const {data,error}=await db.from('matches').select(selectMatch).gte('kickoff_at',new Date().toISOString()).order('kickoff_at').limit(limit);if(error)throw error;return (data||[]) as unknown as MatchRow[]}
export async function dashboard(){const [m,p,o]=await Promise.all([todayMatches(),db.from('prediction_runs').select('*',{count:'exact',head:true}).gte('generated_at',new Date(Date.now()-864e5).toISOString()),db.from('predictions').select('*',{count:'exact',head:true}).gt('edge',0)]);return {matches:m,predictions:p.count||0,opportunities:o.count||0}}
export async function opportunities(){const {data,error}=await db.from('predictions').select('id,market,selection,probability,bookmaker_odd,implied_probability,edge,expected_value,created_at,prediction_run:prediction_runs(match:matches(kickoff_at,home_team:teams!matches_home_team_id_fkey(name),away_team:teams!matches_away_team_id_fkey(name)))').gt('edge',0).order('edge',{ascending:false}).limit(100);if(error)throw error;return data||[]}
